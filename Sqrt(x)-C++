class Solution {
public:
    int mySqrt(int x) {
        unsigned long temp=0;

        for(unsigned int i=0; i<=x;i++)
        {
            temp =  i*i;
            if (temp ==x)

            return i;

            else if (temp>x)
            return i-1;
        }
        return 0;
    }
};
