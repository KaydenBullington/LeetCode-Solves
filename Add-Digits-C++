class Solution {
public:
    int addDigits(int num) {
        while (num >= 10) {
            string s = to_string(num);
            int sum = 0;
            for (char c : s) {
                sum += c - '0';   // convert digit char to int
            }
            num = sum;
        }
        return num;
    }
};
