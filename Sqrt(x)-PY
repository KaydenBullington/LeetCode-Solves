class Solution:
    def mySqrt(self, x: int) -> int:
        for i in range(0,x+1):
            temp = i*i
            if(temp==x):
                return i
            elif(temp>x):
                return(i-1)
        return 0
